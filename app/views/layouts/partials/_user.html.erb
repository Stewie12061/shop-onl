<ul class="navbar-nav mb-2 mb-lg-0">
  <% if user_login? %>
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="<%= destroy_user_session_path %>" data-confirm= "<%= t('users.logout_confirm') %>" data-method='delete'><%=  t('users.logout') %></a>
    </li>
    <% else %>
    <li class="nav-item">
      <a class="nav-link" href="<%= new_user_session_path %>"><%= t('users.login') %></a>
    </li>
    <% end %>
</ul>